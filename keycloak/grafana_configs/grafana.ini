[auth.generic_oauth]
enabled = true
name = Keycloak
allow_sign_up = true
client_id = grafana
client_secret = aK6mMyH33ebSNHsawwo63QBODbK3Wpzp
scopes = openid email profile offline_access roles
email_attribute_path = email
login_attribute_path = username
name_attribute_path = full_name
auth_url = http://localhost:80/realms/grafana-realm/protocol/openid-connect/auth
token_url = http://keycloak:8080/realms/grafana-realm/protocol/openid-connect/token
api_url = http://keycloak:8080/realms/grafana-realm/protocol/openid-connect/userinfo
#role_attribute_path = contains(roles[*], 'admin') && 'Admin' || contains(roles[*], 'editor') && 'Editor' || 'Admin'
role_attribute_path:  (contains(grafana.roles[], 'grafanaAdmin') && 'GrafanaAdmin') || (contains(grafana.roles[], 'admin') && 'Admin') || (contains(grafana.roles[], 'editor') && 'Editor') || 'Viewer'
