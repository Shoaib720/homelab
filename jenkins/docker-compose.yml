networks:
  homelab_network:
    name: homelab_network
    external: true

services:
  jenkins:
    image: jenkins/jenkins:2.521-jdk21
    restart: unless-stopped
    # build:
    #   context: .
    #   dockerfile: Dockerfile
    # ports:
    #   - '8080:8080'
    #   - '50000:50000'
    networks:
      - homelab_network
    volumes:
      - 'jenkins_home:/var/jenkins_home'
      # - '/var/run/docker.sock:/var/run/docker.sock'
    # group_add:
    #   - '997'
    
volumes:
  jenkins_home:
    driver: local